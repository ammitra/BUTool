#ifndef __PRINT_LEVEL_HH__
#define __PRINT_LEVEL_HH__

namespace Level {
    enum level {
        // somewhat according to severity
        INFO = 0,
        DEBUG = 1,
        ERROR = 2
    };
}

#endif